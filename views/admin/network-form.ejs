

<link rel='stylesheet' href='/stylesheets/mou-form.css' />

<div class="mou-add contents-center network-form mt-9">
    <form action="/mou/write" method="post" class="mt-3 mou-form">
        <div class="data-form">
            <div>
                <label for="exampleFormControlInput1" class="required form-label">협력체 명</label>
                <input type="text" name="name" id="name" placeholder="협력체명" class="form-control form-control-solid" value="<% if(typeof(result) == 'object') { %><%= result.name %><% } %>">
            </div>
            <div class="mt-3">
                <label for="exampleFormControlInput1" class="form-label">협력체 주소</label>
                <input type="text" name="adress" id="adress" placeholder="협력체 주소" class="form-control form-control-solid mt-1" value="<% if(typeof(result) == 'object') { %><%= result.adress %><% } %>">
            </div>
            <div class="mt-3">
                <label for="exampleFormControlInput1" class="form-label">협력체 번호</label>
                <input type="text" name="phone-number" id="phone-number" placeholder="협력체 번호(000-0000-0000)" class="form-control form-control-solid mt-1" value="<% if(typeof(result) == 'object') { %><%= result.phone_number %><% } %>">
            </div>
            <div class="mt-3">
                <label for="exampleFormControlInput1" class="form-label">협력체 홈페이지 URL</label>
                <input type="text" name="homepage_url" id="homepage_url" placeholder="협력체 홈페이지 URL" class="form-control form-control-solid mt-1" value="<% if(typeof(result) == 'object') { %><%= result.homepage_url %><% } %>">
            </div>
        </div>
    </form>
    <div class="buttons mt-3 float-right">
        <button type="button" class="btn btn-light" onclick="location.href='/admin/network'">취소</button>
        <% if(typeof(result) == 'object') { %>
            <button type="button" class="btn btn-primary" id="btn-network-update">수정</button>
        <% } else { %>
            <button type="button" class="btn btn-primary" id="btn-network-write">추가</button>
        <% } %>
    </div>
</div>
<script type="text/javascript" src="/javascripts/admin/network.js"></script>
